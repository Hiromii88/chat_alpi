<% favorite = current_user.favorites.find_by(report_id: report.id) %>

<%= link_to report_favorite_path(report_id: report.id, id: favorite.id),
            id: "bookmark-button-for-report-#{report.id}",
            data: { turbo_method: :delete },
            class: "text-yellow-500" do %>
  <i class="bi bi-star-fill"></i>
<% end %>
